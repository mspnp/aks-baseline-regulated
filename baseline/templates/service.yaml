apiVersion: v1
kind: Service
metadata:
  name: {{ include "baseline.fullname" . }}
  labels:
    {{- include "baseline.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "baseline.selectorLabels" . | nindent 4 }}
