apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "baseline.fullname" . }}-test-connection"
  labels:
    {{- include "baseline.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "baseline.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
